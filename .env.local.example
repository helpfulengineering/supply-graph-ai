# =============================================================================
# Open Hardware Manager - Local Storage Configuration Template
# =============================================================================
# This is a minimal configuration template for running OHM with local storage.
# Perfect for getting started, development, testing, and self-hosted setups.
#
# QUICK START:
# 1. Copy this file: cp .env.local.example .env
# 2. Customize storage path if needed (see LOCAL_STORAGE_PATH below)
# 3. Run: ohm storage setup --provider local
# 4. You're ready to use OHM!
#
# For full configuration options, see: env.template
# =============================================================================

# =============================================================================
# Environment Configuration
# =============================================================================
ENVIRONMENT=development

# =============================================================================
# API Configuration
# =============================================================================
DEBUG=false
API_HOST=0.0.0.0
API_PORT=8001
CORS_ORIGINS=*

# API Authentication (optional for local development)
API_KEYS=dev-key-12345
AUTH_MODE=env

# =============================================================================
# LOCAL STORAGE CONFIGURATION
# =============================================================================
# Storage provider - keep this as "local"
STORAGE_PROVIDER=local

# Local storage path - where OHM will store your data
# Choose one of these options:
#
# Option 1: Project directory (default, good for testing)
LOCAL_STORAGE_PATH=./storage
#
# Option 2: Home directory (good for permanent local storage)
# LOCAL_STORAGE_PATH=~/ohm-data
#
# Option 3: Absolute path (good for specific locations)
# LOCAL_STORAGE_PATH=/var/lib/ohm/storage
#
# Option 4: Network storage (good for shared access)
# LOCAL_STORAGE_PATH=/mnt/nas/ohm-storage

# Note: Storage bucket name (used internally, doesn't affect actual location)
STORAGE_BUCKET_NAME=ohm-storage

# =============================================================================
# Logging Configuration
# =============================================================================
LOG_LEVEL=INFO
LOG_FILE=logs/app.log

# =============================================================================
# LLM Configuration (Optional)
# =============================================================================
# Enable LLM features if you want enhanced validation and matching
LLM_ENABLED=false

# If enabling LLM, configure your provider:
# LLM_DEFAULT_PROVIDER=anthropic
# ANTHROPIC_API_KEY=your-api-key-here

# =============================================================================
# Cache and Rate Limiting
# =============================================================================
CACHE_ENABLED=true
CACHE_MAX_SIZE=1000
RATE_LIMIT_ENABLED=true

# =============================================================================
# Domain Configuration
# =============================================================================
COOKING_DOMAIN_ENABLED=true
MANUFACTURING_DOMAIN_ENABLED=true

# =============================================================================
# CLI Configuration
# =============================================================================
OHM_SERVER_URL=http://localhost:8001

# =============================================================================
# Matching Configuration
# =============================================================================
MAX_DEPTH=5

# =============================================================================
# Development/Testing Configuration
# =============================================================================
DEV_MODE=false
TEST_DATA_DIR=test-data

# =============================================================================
# NOTES
# =============================================================================
# - Local storage requires no cloud accounts or credentials
# - Data is stored on your local filesystem or network storage
# - Perfect for development, testing, and self-hosted deployments
# - For production deployments with cloud storage, use env.template instead
# - All paths support: relative (./path), absolute (/path), home (~), env vars ($HOME)
