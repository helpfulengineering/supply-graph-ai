# Repository Code Intelligence Map (Sourcegraph Style)

## Overview

Total files analyzed: 6

## Other

### `audit_env_vars.py`
> Audit environment variables used in code vs documented in env.template.

This script identifies:
- V...

**Exports:** EnvVarUsage, EnvVarAuditor, main

**Classes:**
- `EnvVarUsage`
  - Represents environment variable usage....
- `EnvVarAuditor`
  - Methods: scan_code, scan_template, generate_report
  - Audits environment variable usage....

**Functions:**
- `main()`
  - Run environment variable audit....

### `check_spacy.py`
> Diagnostic script to check spaCy installation and model availability.

This script helps troubleshoo...

**Exports:** check_spacy_installed, check_models, get_recommendations, main

**Functions:**
- `check_spacy_installed()`
  - Check if spaCy is installed....
- `check_models(spacy_module)`
  - Check which spaCy models are available....
- `get_recommendations(available_models)`
  - Provide recommendations based on available models....
- `main()`
  - Main diagnostic function....

### `generate_aider_map.py`
> Generate an Aider-style repository map.
Aider's approach: Tree structure with files and their key sy...

**Exports:** get_git_files, extract_symbols, build_tree_structure, format_tree, generate_aider_map

**Functions:**
- `get_git_files(repo_path)`
  - Get all tracked Python files from git....
- `extract_symbols(file_path)`
  - Extract classes and functions from a Python file using AST....
- `build_tree_structure(files, repo_path)`
  - Build a hierarchical tree structure of the repository....
- `format_tree(tree, indent, prefix)`
  - Format the tree structure as a readable string....
- `generate_aider_map(repo_path)`
  - Generate an Aider-style repository map....

### `generate_sourcegraph_map.py`
> Generate a Sourcegraph-style repository map.
Sourcegraph's approach: Hierarchical symbol extraction ...

**Exports:** get_git_files, extract_detailed_symbols, categorize_files, format_sourcegraph_map, generate_sourcegraph_map

**Functions:**
- `get_git_files(repo_path)`
  - Get all tracked Python files from git....
- `extract_detailed_symbols(file_path, repo_path)`
  - Extract detailed symbol information including imports and relationships....
- `categorize_files(symbols_by_file)`
  - Categorize files by their role in the codebase....
- `format_sourcegraph_map(categories)`
  - Format the Sourcegraph-style map as markdown....
- `generate_sourcegraph_map(repo_path)`
  - Generate a Sourcegraph-style repository map....

### `setup_storage.py`
> Standalone storage setup script for bootstrapping new environments.

This script can be run independ...

**Internal Dependencies:** 3 imports

### `validate_docs.py`
> Validate documentation against code implementation.

This script checks:
- Authentication header for...

**Exports:** DocValidator, main

**Classes:**
- `DocValidator`
  - Methods: check_auth_header, check_port_numbers, generate_report
  - Validates documentation against code....

**Functions:**
- `main()`
  - Run documentation validation....
